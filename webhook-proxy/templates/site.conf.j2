server {
  listen 80;
  listen [::]:80;
  server_name {{ domain }} www.{{ domain }};
  root /var/www/{{ domain }};
  location / {
    try_files $uri $uri/ =404;
  }
  location /openshift/ {
    proxy_pass {{ openshift_url }};
  }
}
